<div class="voki-taking-needed">To rate this Voki you need to take it first</div>

<style>
	.voki-taking-needed {
		margin: 0.25rem 1rem;
		width: calc(100% - 2rem);
		padding: 1rem;
		border-radius: 0.5rem;
		box-shadow: var(--shadow-xs);
		background-color: var(--secondary);
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
		font-weight: 500;
		font-size: 1.5rem;
		color: var(--secondary-foreground);
	}
</style>
